const simpleStrategy = require('./simpleStrategy.ts');
const intermediateStrategy = require('./intermediateStrategy');
const advancedStrategy = require('./advancedStrategy');

module.exports = {
  simpleStrategy,
  intermediateStrategy,
  advancedStrategy,
};